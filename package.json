{
  "name": "arena-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "把本机项目通过 MCP 交给网页 AI Agent，并在浏览器里实时显示抽到的真实模型名。零依赖。",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/cli.mjs",
    "readonly": "node server/cli.mjs --readonly",
    "write": "node server/cli.mjs --write",
    "full": "node server/cli.mjs --write --exec"
  },
  "license": "MIT",
  "devDependencies": {
    "electron": "^44.4.2"
  }
}
